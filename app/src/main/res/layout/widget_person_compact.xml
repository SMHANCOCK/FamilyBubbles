<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/personRoot"
    android:layout_width="0dp"
    android:layout_height="match_parent"
    android:layout_weight="1"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingLeft="1dp"
    android:paddingRight="1dp">

    <ImageView
        android:id="@+id/personPhoto"
        android:layout_width="@dimen/widget_face_compact_width"
        android:layout_height="@dimen/widget_face_compact_height"
        android:contentDescription="@string/call_family_member"
        android:scaleType="fitCenter" />

    <TextView
        android:id="@+id/personName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:fontFamily="sans-serif-rounded"
        android:gravity="center"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textColor="@color/family_navy"
        android:textSize="11.5sp"
        android:textStyle="bold" />
</LinearLayout>
