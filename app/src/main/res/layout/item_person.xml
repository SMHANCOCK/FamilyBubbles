<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:background="@drawable/bg_member_card"
    android:elevation="2dp"
    android:gravity="center_vertical"
    android:minHeight="98dp"
    android:orientation="horizontal"
    android:paddingStart="14dp"
    android:paddingTop="13dp"
    android:paddingEnd="12dp"
    android:paddingBottom="13dp">

    <ImageView
        android:id="@+id/photoView"
        android:layout_width="@dimen/member_avatar_size"
        android:layout_height="@dimen/member_avatar_size"
        android:background="@drawable/bg_photo_placeholder"
        android:contentDescription="@string/contact_photo_description"
        android:scaleType="centerCrop" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="14dp"
        android:layout_marginEnd="8dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/nameText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="sans-serif-rounded"
            android:maxLines="2"
            android:textColor="@color/family_navy"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/phoneText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:ellipsize="end"
            android:fontFamily="sans-serif-rounded"
            android:maxLines="1"
            android:textColor="@color/text_secondary"
            android:textSize="14sp" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/editButton"
        android:layout_width="@dimen/action_touch_size"
        android:layout_height="@dimen/action_touch_size"
        android:layout_marginEnd="8dp"
        android:background="@drawable/bg_edit_action"
        android:contentDescription="@string/edit_generic"
        android:padding="14dp"
        android:src="@drawable/ic_edit_blue" />

    <ImageButton
        android:id="@+id/deleteButton"
        android:layout_width="@dimen/action_touch_size"
        android:layout_height="@dimen/action_touch_size"
        android:background="@drawable/bg_delete_action"
        android:contentDescription="@string/delete_generic"
        android:padding="14dp"
        android:src="@drawable/ic_delete_pink" />
</LinearLayout>
